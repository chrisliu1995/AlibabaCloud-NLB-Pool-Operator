---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: listeners.nlboperator.alibabacloud.com
spec:
  group: nlboperator.alibabacloud.com
  names:
    kind: Listener
    listKind: ListenerList
    plural: listeners
    singular: listener
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Listener is the Schema for the listeners API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ListenerSpec defines the desired state of Listener
            properties:
              listenerPort:
                description: ListenerPort is the listener front-end port.
                format: int32
                type: integer
              listenerProtocol:
                description: 'ListenerProtocol: TCP | UDP | TCPSSL'
                type: string
              loadBalancerRef:
                description: LoadBalancerRef references the NLB CR name in the same
                  namespace.
                type: string
              region:
                type: string
              serverGroupRef:
                description: ServerGroupRef references the ServerGroup CR name in
                  the same namespace.
                type: string
            required:
            - listenerPort
            - listenerProtocol
            - loadBalancerRef
            - region
            - serverGroupRef
            type: object
          status:
            description: ListenerStatus defines the observed state of Listener
            properties:
              listenerId:
                type: string
              message:
                type: string
              phase:
                description: ListenerPhase represents the lifecycle phase of a Listener
                  CR.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
