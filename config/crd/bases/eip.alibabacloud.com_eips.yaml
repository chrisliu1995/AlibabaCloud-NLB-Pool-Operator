---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: eips.eip.alibabacloud.com
spec:
  group: eip.alibabacloud.com
  names:
    kind: EIP
    listKind: EIPList
    plural: eips
    singular: eip
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EIP is the Schema for the eips API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EIPSpec defines the desired state of EIP (simplified for
              pool operator)
            properties:
              bandwidth:
                type: string
              description:
                type: string
              internetChargeType:
                type: string
              isp:
                type: string
              name:
                type: string
              releaseStrategy:
                type: string
              securityProtectionTypes:
                items:
                  type: string
                type: array
            type: object
          status:
            description: EIPStatus defines the observed state of EIP (simplified for
              pool operator)
            properties:
              allocationID:
                type: string
              eipAddress:
                type: string
              status:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
