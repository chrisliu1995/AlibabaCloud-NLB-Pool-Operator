---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: nlbs.nlboperator.alibabacloud.com
spec:
  group: nlboperator.alibabacloud.com
  names:
    kind: NLB
    listKind: NLBList
    plural: nlbs
    singular: nlb
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: NLB is the Schema for the nlbs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NLBSpec defines the desired state of NLB (simplified for
              pool operator)
            properties:
              addressIpVersion:
                type: string
              addressType:
                type: string
              loadBalancerName:
                type: string
              vpcId:
                type: string
              zoneMappings:
                items:
                  description: ZoneMapping defines the zone and vSwitch configuration
                    for NLB
                  properties:
                    allocationId:
                      type: string
                    vSwitchId:
                      type: string
                    zoneId:
                      type: string
                  required:
                  - vSwitchId
                  - zoneId
                  type: object
                type: array
            required:
            - addressType
            - vpcId
            - zoneMappings
            type: object
          status:
            description: NLBStatus defines the observed state of NLB (simplified for
              pool operator)
            properties:
              dnsName:
                type: string
              loadBalancerId:
                type: string
              loadBalancerStatus:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
