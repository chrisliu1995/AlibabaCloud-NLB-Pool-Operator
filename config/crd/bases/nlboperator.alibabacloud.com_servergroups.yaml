---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: servergroups.nlboperator.alibabacloud.com
spec:
  group: nlboperator.alibabacloud.com
  names:
    kind: ServerGroup
    listKind: ServerGroupList
    plural: servergroups
    singular: servergroup
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: ServerGroup is the Schema for the servergroups API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServerGroupSpec defines the desired state of ServerGroup
            properties:
              protocol:
                description: 'Protocol: TCP | UDP | TCPSSL'
                type: string
              region:
                type: string
              scheduler:
                type: string
              serverGroupName:
                type: string
              serverGroupType:
                description: 'ServerGroupType: Ip | Instance'
                type: string
              vpcId:
                type: string
            required:
            - protocol
            - region
            - serverGroupName
            - serverGroupType
            - vpcId
            type: object
          status:
            description: ServerGroupStatus defines the observed state of ServerGroup
            properties:
              message:
                type: string
              phase:
                description: ServerGroupPhase represents the lifecycle phase of a
                  ServerGroup CR.
                type: string
              serverGroupId:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
